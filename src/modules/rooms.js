// Placeholder — US-21 (CRUD de Salas) ainda nao implementada.
// A pagina é registrada em PAGE_RENDERERS para nao quebrar a navegacao,
// mas renderiza um container vazio ate a feature ser entregue.
export function renderRooms() {
  return document.createElement('div');
}
