// Placeholder — US-22 (CRUD de Usuarios) e US-23 (Aprovacao de cadastros)
// ainda nao implementadas. A pagina é registrada em PAGE_RENDERERS para
// nao quebrar a navegacao, mas renderiza um container vazio ate a feature
// ser entregue.
export function renderUsers() {
  return document.createElement('div');
}
